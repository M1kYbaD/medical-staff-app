<h1 align="center">MedicalStaffApp 👨🏻‍⚕️</h1>

<p align="center">
  <img src="public/angular_icon_gradient.gif" alt="angular-logo" width="150px" height="150px"/>
  <br>
  <em>MedicalStaffApp è un’applicazione web sviluppata in Angular per la gestione e la consultazione del personale medico e sanitario.
    <br> Consente di visualizzare, filtrare e navigare i dati del personale in modo semplice e intuitivo.</em>
  <br>
</p>

---

## Server di sviluppo

Per avviare un server di sviluppo locale, esegui:

```bash
ng serve
```

Una volta avviato il server, apri il browser e vai su `http://localhost:4200/`. L’applicazione si ricaricherà automaticamente ogni volta che modifichi uno dei file sorgente.

## Generazione del codice (**scaffolding**)

Angular CLI include potenti strumenti di scaffolding.
Per generare un nuovo componente, esegui:

```bash
ng generate component nome-componente
```

Per un elenco completo degli schematics disponibili (come `components`, `directives`, o `pipes`), esegui:

```bash
ng generate --help
```

## Build

Per creare la build del progetto, esegui:

```bash
ng build
```

Questo comando compilerà il progetto e salverà gli artefatti di build nella directory `dist/`.
Per impostazione predefinita, la build di produzione ottimizza l’applicazione in termini di prestazioni e velocità.

## Esecuzione dei test unitari

Per eseguire i test unitari con il **test runner** [**Karma**](https://karma-runner.github.io) utilizza il seguente comando:

```bash
ng test
```

## Esecuzione dei test end-to-end

Per i test **end-to-end** (e2e), esegui:

```bash
ng e2e
```

Angular CLI non include di default un framework per i test end-to-end.
Puoi scegliere quello più adatto alle tue esigenze.

## Risorse aggiuntive

Per tutorial e documentazione, visita [**Angular**](https://angular.dev/overview).

Per maggiori informazioni sull’uso della Angular CLI, incluse le referenze dettagliate dei comandi, visita la pagina [**Angular CLI Overview and Command Reference**](https://angular.dev/tools/cli).

- **Questo progetto è stato generato utilizzando [Angular CLI](https://github.com/angular/angular-cli) `v20.3.8`**

---

## User Guide

Per i dettagli di utilizzo (`search tips`, `filters`, `conventions`), consulta la [**User Guide**](docs/USER_GUIDE.md).

🌍 Leggilo in:

- [English](README.md)
- [Italiano](README.it.md)
